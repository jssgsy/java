# 邮件服务器 SMTP 地址（不同邮箱不同）
host = smtp.163.com

# 端口（SSL 端口，推荐优先使用）
port = 465

# 发送者邮箱账号（你的邮箱）
from = dailyplanc@163.com

# 发送者邮箱密码（注意：不是登录密码，是 SMTP 授权码！）
pass = xxx

# 是否开启 SSL（端口 465 时必须为 true，25 端口为 false）
sslEnable = true
